formatters:
  uppercase:
    command: tr '[:lower:]' '[:upper:]'
    patterns:
      - '!*.skip'
      - '*'
      - '!packages/patchnote/templates/**'

  suffix:
    command: sed 's/$/ FORMATTED/'
    patterns:
      - '*.file'
